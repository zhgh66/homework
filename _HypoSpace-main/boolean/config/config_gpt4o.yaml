llm:
  type: openrouter
  models:
    openrouter: "openai/gpt-4o"
  api_keys:
    openrouter: ""
  temperature: 0.7

benchmark:
  checkpoint: "checkpoints"
  verbose: true
  output_pattern: "results/{dataset_name}_{model}.json"
